apiVersion: v1
kind: Service
metadata:
  name: users        # ← this name IS the DNS name the gateway resolves
spec:
  type: ClusterIP
  selector:
    app: users
  ports:
    - port: 8000
      targetPort: 8000
